export type MatchPhase = "mulligan" | "turnStart" | "draw" | "roll" | "roulette" | "fieldCombat" | "main" | "turnEnd" | "enemy" | "ended";
export type PlayerId = "player" | "enemy";
export type CardType = "character" | "event" | "attack" | "effect" | "tactic" | "upgrade" | "bonus";
export type CardRarity = "common" | "rare" | "epic" | "legendary" | "mythic" | "chromatic" | "exotic" | "divine";
export type EffectTarget = "selfHero" | "enemyHero" | "frontEnemyCard" | "randomEnemy" | "opponentDeck" | "self" | "enemy";
export type CardEffect = { op: string; target?: EffectTarget; value?: number; key?: string; percent?: number };
export type ScalableField = "damage" | "healing" | "armor" | "cardHp";
export type CardElement = "fire" | "ice" | "lightning" | "water" | "void" | "neutral";
export type CardDefinition = { id: string; title: string; ruTitle?: string; type: CardType; rarity: CardRarity; cost: number; willCost?: number; attack: number; health: number; hp?: number; armor?: number; element?: CardElement; description: string; image: string; effectKey?: string; effects?: CardEffect[]; collection?: string; requiresBoardSlot?: boolean; leavesAtTurnEnd?: boolean; scalableByElementBonus?: boolean; scalableFields?: ScalableField[]; canAttack?: boolean; canCounterAttack?: boolean; reflectDamage?: number; piercing?: boolean; duration?: number; } ;
export type CardInstance = { instanceId: string; baseId: string; definition: CardDefinition; currentAttack: number; currentHealth: number; currentArmor?: number; serial?: string; foil?: boolean; ownerId: PlayerId; originalOwnerId?: PlayerId; controllerId?: PlayerId; slotIndex?: number; enteredFieldOnTurn?: number; canAttackFromTurn?: number; duration?: number; statuses?: string[]; revealed?: boolean; temporaryCost?: number; copiedFromInstanceId?: string; temporaryUntilRoundEnd?: boolean; playedRound?: number };
export type ActiveEffect = { id: string; sourceId: string; label: string; value?: number; turns?: number };
export type PendingEffect = { sourceId: string; effect: CardEffect; playerId: PlayerId };
export type TargetRef = { type: "hero" | "slot"; playerId: PlayerId; slotIndex?: number };
export type PlayerState = { id: PlayerId; hero: CardInstance; hp: number; maxHp: number; shield: number; will: number; maxWill: number; willRegenPerRound?: number; personalTurnsTaken?: number; deck: CardInstance[]; hand: CardInstance[]; discard: CardInstance[]; exile?: CardInstance[]; bonusCards: CardInstance[]; effects: ActiveEffect[]; lastTurnLostHp: number };
export type MatchState = { id: string; phase: MatchPhase; turn: number; activePlayerId: PlayerId; player: PlayerState; enemy: PlayerState; board: { playerSlots: Array<CardInstance | null>; enemySlots: Array<CardInstance | null> }; stack: PendingEffect[]; log: string[]; rngSeed: number; winner?: "player" | "enemy" | "draw"; lastRoll?: number; initiativeRolls?: Array<{ player: number; enemy: number }>; battleNumber?: number; seriesScore?: Record<PlayerId, number>; caduceusUsed?: boolean; rouletteUsedThisBattle?: boolean; activeRouletteEvent?: string; currentTurn?: { playerId: PlayerId; d20Limit: number | "unlimited"; playsUsed: number; cardsPlayed: number; destroyedOwnCard: boolean; freeCards: boolean; skipDamageApplied: boolean; timerSeconds: number; playedCosts: Array<{ playerId: PlayerId; cardTitle: string; cost: number }> }; sharedDeck?: CardInstance[]; } ;
export type StartMatchPayload = { seed?: number; playerDeck?: CardDefinition[]; enemyDeck?: CardDefinition[] };
